# This should not raise an error
# Testing if the parser can verify a return statement will
# be hit.

begin
  # Function declaration
  bool dance (bool happy) is
    if happy then
      return true
    else
      println "Screw this";
      return false
    fi;
    skip   # should not be executed
  end

  # Call dance, implicitly return
  dance true
end
